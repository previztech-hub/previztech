This `public` folder contains static assets served at the root of the application.

Files added:
- `favicon.svg` - simple SVG favicon
- `robots.txt` - crawling rules
- `manifest.json` - basic web app manifest
- `og-image.svg` - placeholder Open Graph image

Replace these with your production assets (PNG/JPG/ICO) as needed.
